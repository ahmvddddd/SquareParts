import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Responsive {

  //Width
   static double horizontalCardWidth = MediaQuery.of(Get.context!).size.width * 0.70;
   static double verticalCardWidth = MediaQuery.of(Get.context!).size.width * 0.45;
   static double searchContainerWidth = MediaQuery.of(Get.context!).size.width * 0.90;
   static double searchMediumWidth = MediaQuery.of(Get.context!).size.width * 0.80;
   static double tabIconWidth = MediaQuery.of(Get.context!).size.width * 0.15;
   static double promoSliderWidth = MediaQuery.of(Get.context!).size.width * 0.90;
   static double cardWidth = MediaQuery.of(Get.context!).size.width * 0.35;
   static double smCardWidth = MediaQuery.of(Get.context!).size.width * 0.25;
   static double xsCardWidth = MediaQuery.of(Get.context!).size.width * 0.15;
   static double imageCardWidth = MediaQuery.of(Get.context!).size.width * 0.90;
   static double buttonMediumWidth = MediaQuery.of(Get.context!).size.width * 0.45;


   //height
   static double horizontalCardHeight = MediaQuery.of(Get.context!).size.height * 0.20;
   static double mdHorizontalCardHeight = MediaQuery.of(Get.context!).size.height * 0.18;
   static double verticalCardHeight = MediaQuery.of(Get.context!).size.height * 0.25;
   static double tabIconHeight = MediaQuery.of(Get.context!).size.height * 0.065;
   static double promoSliderHeight = MediaQuery.of(Get.context!).size.height * 0.25;
   static double cardHeight = MediaQuery.of(Get.context!).size.height * 0.28;
   static double smCardHeight = MediaQuery.of(Get.context!).size.height * 0.12;
   static double xsCardHeight = MediaQuery.of(Get.context!).size.height * 0.8;
   static double imageCardHeight = MediaQuery.of(Get.context!).size.height * 0.30;
   static double buttonHeight= MediaQuery.of(Get.context!).size.height * 0.05;
   static double searchContainerHeight = MediaQuery.of(Get.context!).size.height * 0.055;

}